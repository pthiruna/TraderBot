I want to implement an investment strategy. I then want to run the investment strategy on historic data and gather metrics on how it performs. First create a strategy folder where the code for the strategy would reside. Also create a strategy plan folder where we will store the markdown file that describes the strategy. The first strategy we will implement is based on reversal to mean approarch. This is common strategy used by traders that rely on both the 50 day moving average and the 200 day moving average to determine when to buy or sell the asset. Do a research online about this strategy and determine the thresholds that will trigger a buy or sell and also the percentage of total assets that will be bought or sold. Once we implement the strategy we will execute it. Execution will run our strategy on the historic data  one day at a time and capture the asset value at the end of each day in a csv file. put the file in the strategy execution output folder. Now in the web app add a feature to execute a given strategy. Add a new page that will have a dropdown which will allow us to pick a strategy. Another drop down to pick a ticker. A date range. Default date range will be max for a given ticker. The historic price data point is available in the data/ticker folder use it.


